# Plan: Give The Script To The Thing That Runs It

## Порядок

1. Написать скрипт в файл.
2. Назвать файл в `SSH_STDIN`.

## Риски

**Такое же место в другом workflow.** Проверено: остальные вызовы, которым нужен ввод, уже передают его через `SSH_STDIN`.

## Валидация

- запуск печатает четыре звена цепи
