# Plan: Откат, который существует

## Порядок

1. Отказ без напечатанного `ROLLBACK` и без имени вида `*_before_*`.
2. Проверить, что названная база существует, — до остановки сервиса.
3. Остановить сервис, отставить текущую под датированным именем, вернуть прежнюю.
4. Запустить и проверить `204`.

## Риски

**Опечатка в имени.** Остановила бы сервис и не оставила бы, на чём поднять. Существование базы проверяется до остановки.

**Откат окажется ошибкой.** Тоже обратим: та база, что не заработала, остаётся на диске.

**Снятый предохранитель расползётся.** Отсутствие обязательной копии — только здесь и только потому, что копию в этом положении снять нельзя. В `restore-live.yml` требование остаётся.

## Валидация

Живой прогон: вернуть `controlplane_before_20260901151553` и увидеть `204`.
