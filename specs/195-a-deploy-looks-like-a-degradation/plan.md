# Plan: Выкат, похожий на деградацию

## Порядок

1. Сверить панель с тем, чего ждали от правки вердиктов.
2. Перенести закрытый долг, сохранив причину, по которой тест его не ловил.
3. Записать находку про пути входа вместе с неизмеренным.
4. Записать то, что делает путаницу возможной.

## Риски

**Объяснение принято за проверку.** В записи прямо сказано, чем оно подтвердится и чем опровергнется: возвратом обоих адресов в `works` без выкатов.

**Находка растворится среди долгов.** У неё названо и что закрывает: вердикт, не считающий окно перезапуска, либо называющий его отдельно.

## Валидация

Кода нет. Проверяется baseline. Настоящая проверка находки — следующее чтение панели через несколько часов без выкатов.
