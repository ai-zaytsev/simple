# Plan: Lightweight ACME And Early Status

- Feature ID: `020-node-lightweight-acme`
- Feature Branch: `feature/020-node-lightweight-acme`
- Owner: `orchestrator`

## Implementation Slices

1. Убрать Certbot из пакетов.
2. Перенести сервер статуса в `bootcmd`.
3. Поставить `lego` закреплённой версии с проверкой контрольной суммы.
4. Перевести Nginx на пути сертификатов `lego`.
5. Прогон.

## Risks

- имена артефактов релиза `lego` содержат версию и с префиксом `v`, и без него: закреплены оба варианта, расхождение поймает проверка контрольной суммы
- экранирование `$${` в шаблоне легко перепутать: локальный рендер повторяет правило Terraform и проверяет отсутствие нерешённых подстановок
- сервер статуса занимает порт 80 до старта Nginx: снимается по сохранённому pid

## Validation Plan

- рендер шаблона локально с учётом экранирования, проверка YAML
- прогон с уборкой

## Merge Readiness

Закрывается прогоном до `ready` и подтверждением обоих транспортов.
