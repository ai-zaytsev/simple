# Plan: Look At The Panel After Deploy

## Порядок

1. Записать правило туда, где описано завершение работы.
2. Записать причину рядом с правилом.

## Риски

**Правило без причины забывается первым.** Поэтому оба случая названы прямо в тексте, с числами.

**Два контракта завершения расходятся.** `AGENTS.md` ссылается на `CLAUDE.md`, а не повторяет текст.

## Валидация

Проверяется не тестом, а тем, что следующий деплой сопровождается сверкой.
