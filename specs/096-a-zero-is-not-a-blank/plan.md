# Plan: A Zero Is Not A Blank

## Порядок

1. Убрать все подстановки через `or` там, где ноль осмыслен.
2. Соединить решённое и измеренное в таблице точек входа.
3. Добавить в чтение мощности размах истории.
4. Зафиксировать живую форму как фикстуру и проверять вывод, а не только выход.

## Риски

**Такой же дефект в другом месте.** Проверены все `or` в читалке: их было шесть, осмысленный ноль возможен во всех.

**Guard, доказывающий не то.** Прежний доказывал, что читалка не падает, и пропустил ошибку в том, что она печатает. Новый сравнивает вывод со строкой.

## Валидация

- baseline: фикстура с нулями печатает `0%`, а не прочерк
- baseline: пики называют размах истории
- следующее живое чтение
