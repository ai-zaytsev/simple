# Plan: Чек — обязанность, а не настройка

## Порядок

1. Долг про чек НПД в `docs/tech-debt.md`.
2. Ответы Business Owner в спецификацию переключения.
3. Статус 190 — `merged-not-yet-exercised`: выкат идёт, покупки ещё не было.

## Риски

**Долг выглядит как задача разработки и потеряется среди них.** В формулировке сказано прямо, что ЮKassa тут ни при чём и чинить в коде оплаты нечего.

**Дата начала разрыва не зафиксирована.** Записана дата решения. Дата первой продажи станет известна из первой же записи в `payments` и в тот момент будет точнее любой оценки.

## Валидация

Изменений в коде нет. Проверяется baseline.
