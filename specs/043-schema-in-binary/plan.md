# Plan: Schema In The Binary

1. Перенести миграции в `internal/store/migrations/`, вкомпилировать через `embed`.
2. Мигратор: лок, журнал, транзакция на файл.
3. Вызов при старте, до прослушивания порта.
4. Развёртывание передаёт почтовую конфигурацию.
5. Развёртывание проверяет `/a` снаружи.

## Риск

Миграция, применённая руками и не записанная в журнал, выполнится повторно. Обе существующие для этого и написаны; для будущих это перестанет быть требованием.
