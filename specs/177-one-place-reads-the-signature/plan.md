# Plan: Одно место читает подпись

## Порядок

1. Вынести чтение отпечатка в `.github/scripts/apk-certificate.sh`.
2. Заменить обе копии вызовом.
3. Печатать оба отпечатка при несовпадении.
4. Повторить публикацию — как синхронизацию с Core.

## Риски

**Появится третья копия.** Проверено, что привязанного шаблона в репозитории больше нет, а вызовов ровно два.

**Повторный запуск перезапишет релиз.** Не перезапишет: workflow сам обнаруживает опубликованную версию, требует совпадения versionName, versionCode и сертификата и отказывается трогать неизменяемый объект.

**Сборка даст другие байты.** Даст — и именно поэтому переиспользуется уже опубликованная личность релиза, а не новая.

## Валидация

Скрипт испытан на трёх образцах: старая форма, новая с двумя блоками, подделка с двумя сертификатами.

Затем запуск: должен пройти публичную проверку и выполнить регистрацию в Core.
