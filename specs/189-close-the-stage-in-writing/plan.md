# Plan: Закрыть стадию письменно

## Порядок

1. Записать оставленные базы в долг, вместе с тем, что не измерено.
2. Записать непроверенную проверку здоровья в долг.
3. Перенести «Нет PostgreSQL backup и проверенного restore» в закрытые.
4. Развести статусы: `live-accepted` против `merged-not-yet-exercised`.

## Риски

**Долг записан и на этом успокоились.** У обоих пунктов сказано, чем закрывается, и ни один не закрывается сам по себе. Пункт про базы ждёт инструмента, пункт про проверку — первого выката.

**`merged-not-yet-exercised` — новое слово в словаре статусов.** В спеках уже тринадцать разных значений, и это четырнадцатое. Оправдано тем, что описывает состояние, которого нет ни у одного существующего: в `main`, но ни разу не выполнено. Приводить словарь в порядок — отдельная работа, и она в долге не значится.

## Валидация

Изменений в коде нет. Проверяется baseline.
