# Plan: Take Back What Was Not Asked For

## Порядок

1. Взять состояние из непопавшего в `main` коммита.
2. Проверить, что `replyTo` и `CP_REPLY_TO` не остались нигде.
3. Поправить спецификацию 094.

## Риски

**Снести заодно то, что оставлять надо.** Исправления документации в 094 верны и сохраняются; убирается только код отправки и то, что его описывало.

**Сломать вход.** Путь отправки письма входа — единственный, который ломать нельзя. Возвращается ровно то состояние, которое работало до, а не переписывается заново.

## Валидация

- поиск по `replyTo` и `CP_REPLY_TO` не находит ничего
- baseline
