# Технический Долг

То, что известно, не сделано и не блокирует релиз. Всё, что блокирует, живёт в [release-blockers.md](release-blockers.md) — здесь только то, с чем можно выкатываться, но нельзя забыть.

Отдельный документ, потому что долг, оставшийся в голове или в чате, перестаёт существовать при первой же смене контекста. Каждый пункт называет, чем он рискует и что закрывает — «сделать нормально» это не пункт.

Правило заведения: то, что не удалось выяснить за один проход, попадает сюда, а не в память.

## Инфраструктура

### Node Access проверяет не production-путь CI

`Update Node` и `Node Inspect` штатно входят на обе живые ноды через Core как jump host. Старое утверждение «ноды доступны только с личной машины Business Owner» неверно и удалено.

**Риск:** отдельный workflow `Node Access` проверяет другой ключ и может показывать отказ при полностью рабочем production-пути. Такая диагностика создаёт ложный инцидент и не доказывает доступ, которым реально пользуется CI.

**Закрывается:** `Node Access` использует тот же jump-host/key path, что Update/Inspect, либо удалён как не отвечающий эксплуатационному вопросу.

### Обращения к Kamatera написаны в трёх местах

**Риск:** одни и те же вызовы к провайдеру живут в `add-node.yml`, `kamatera-node.yml` и общем `infra/kamatera.sh`. Каждый из них знает про идентификатор в теле, про поиск адреса по виду и про то, что удаление подтверждается списком, — и знает по отдельности. Трижды за одну сессию эти знания были изобретены заново, каждый раз ценой запуска.

**Что сделано:** `infra/kamatera.sh` заведён и используется `retire-node.yml`.

**Закрывается:** `add-node.yml` и `kamatera-node.yml` ходят к провайдеру через тот же файл.

### Порт 22 Control Plane открыт всем

**Риск:** непрерывный перебор паролей идёт по этому порту с первого дня. Вход по паролю закрыт, так что взлома нет, но поверхность есть.
**Почему не закрыто:** конвейер приходит с адресов GitHub Actions, которых заранее не перечислить.
**Закрывается:** список адресов сужен либо доступ идёт через посредника с фиксированным адресом.

### Конфигурация nginx перед Control Plane правится руками

**Риск:** машина, восстановленная с нуля, получит не тот nginx. Ни один тест этого не заметит, потому что конфигурации нет в репозитории.
**Закрывается:** конфигурация лежит в репозитории и применяется выкатом.

### Выкат Control Plane не переживает обрыв SSH

**Риск:** случайный обрыв роняет выкат. Уже случилось: 2026-08-28 выкат упал с `Connection closed`, повтор без правок прошёл.
**Почему странно:** `infra/ssh-retry.sh` написан ровно под это и применён только к нодам.
**Закрывается:** `deploy-control-plane.yml` ходит через `ssh-retry.sh`.

### Четвёртый расходный домен

**Риск:** свободный расходный домен остался один, и мощность из-за этого в WATCH. Четвёртую ноду поднимать не на что.
**Закрывается:** домен куплен Business Owner и заведён в инвентаре.

### Отменённые восстановления оставляют базы на диске

**Риск:** `restore-live.yml` ничего не удаляет — это его главное свойство, — но у него есть побочный эффект: каждый прогон, включая отменённые и упавшие, оставляет на fi базу `simple_vpn_restored_*`, `controlplane_before_*` или `controlplane_failed_*`. Их накопилось несколько за одну отладку. Диск fi — 10 ГБ, тот же, на котором живёт боевая база; заполнение диска остановит Core, а не только восстановление.

**Обнаружено:** отладка живого восстановления 2026-09-01, прогоны 33538–33542.

**Не измерено:** сколько именно баз лежит и сколько занимают. У локальной среды нет SSH к fi, а гнать инфраструктурный workflow ради подсчёта не стали.

**Закрывается:** есть способ увидеть список оставленных баз и их размер, и есть чем убрать выбранную по имени — с тем же подтверждением, что у отката, потому что удаление базы необратимо. До тех пор оставленное убирается руками.

### Вердикт по пути входа переезжает порог от тишины, а не от поломки

**Риск:** доля успеха считается по фиксированному окну в 24 часа. При одном активном пользователе окно за тихие часы теряет успешные замеры, а старые отказы в нём остаются — и вердикт переходит из `works` в `slower`, хотя с адресом ничего не произошло.

Наблюдалось 2026-09-02. Числа:

| адрес | вчера 07:15 | сегодня 13:04 | отказов было → стало |
| --- | --- | --- | --- |
| №1 | 70 проверок, 90% | 31 проверка, 77% | 7,0 → 7,1 |
| №5 | 92 проверки, 92% | 39 проверок, 85% | 7,4 → 5,9 |

Отказы не изменились. Успехи упали с 63 до 24 и с 85 до 33. Просел знаменатель.

Порог `enough = 10` от этого не защищает: 31 замер его проходит и всё равно слишком мал, чтобы семь старых отказов не решали исход.

**Чего это стоит:** пока так, настоящая деградация пути входа в тихий период неотличима от этой тени, а тихий период — норма при одном пользователе. Первым же следствием была неверная догадка: списал переход на собственные выкаты, и только счёт отказов показал, что новых отказов не было вовсе.

**Обнаружено:** сверка панели после выката 2026-09-02, прогоны `33633258133` и чтения панели 07:15 и 13:04.

**Закрывается:** вердикт перестаёт быть долей от малой выборки — либо панель показывает счёт отказов рядом с долей, чтобы «семь отказов из тридцати одного» нельзя было прочитать как ухудшение, либо доля считается по окну, в котором замеров достаточно, а не по окну фиксированной длины.

### Запасной путь входа не отвечает

**Риск:** в панели после выката 2026-09-02 точка входа №2 значится запасом, состояние `unreachable`, проверок 0, помечена «заменить». То есть резерва входа фактически нет: если рабочий путь заблокируют, переключаться не на что. Мощность при этом в WATCH, свободный расходный домен один.

**Обнаружено:** сверка панели после выката на боевой магазин, 2026-09-02. Панель показала это сама — она для этого и сделана.

**Закрывается:** запасной путь входа отвечает с устройств, и у него ненулевое число проверок.

### У рабочих нод домен «не измерен»

**Риск:** обе ноды в состоянии `ok` и выдаются, но в столбце «домен» стоит «не измерен», хотя их прикрытия отвечают. Связь «нода ↔ её домен» на панели не подтверждена, поэтому неизвестно, какое прикрытие проверять при отказе конкретной ноды.

**Обнаружено:** та же сверка 2026-09-02.

**Закрывается:** в панели у работающей ноды виден её домен и результат его проверки.

### Нет host monitoring для Core, PostgreSQL и APK site

**Риск:** panel видит приложение и VPN-ноды, но не предупреждает о заполнении диска, memory pressure или остановке Nginx на fi/site-1. Отказ обнаруживается пользовательским 5xx или ручной проверкой.

**Закрывается:** безопасные host metrics/alerts с порогами из BO-инструкции, без пользовательских идентификаторов и публичной панели.

### Journald не архивируется вне хоста

**Риск:** после ротации или потери машины техническая причина инцидента исчезает. Prefix logs/ в Spaces подготовлен, но upload/delete job отсутствует.

**Закрывается:** ограниченная по сроку выгрузка безопасного формата, privacy test, проверка чтения архива и документированный retention.

### Журнал ноды печатается без времени

**Риск:** `Node Inspect` показывает журнал агента через `journalctl -o cat` — строки без отметок времени. 2026-08-29 последними строками были `could not deliver: TimeoutError` и `URLError`, и сказать по ним, происходит это сейчас или случилось при перезапуске полчаса назад, было нечем. Ответ пришлось брать косвенно: панель показывала свежие CPU и задержку, значит доставка идёт.

Диагностика, не отвечающая «когда», не отличает текущий отказ от давнего — то есть ровно в том случае, ради которого её открывают, она молчит.

**Закрывается:** осмотр печатает журнал с отметками времени.

### Записи о выполнении в specs устарели

**Риск:** в шестнадцати папках `specs/120`–`specs/135` остались невыполненные пункты, которые на самом деле выполнены — «нода отдаёт боевой сертификат», «обновление ноды проходит», «осмотр запустился». Всё это произошло 2026-08-29 и в `tasks.md` не отражено.

CLAUDE.md называет `tasks.md` фактическим состоянием выполнения. Пока в нём вперемешку настоящие открытые пункты и остывшие, на вопрос «завершена ли стадия» по репозиторию ответить нельзя — а именно этот вопрос и задают. Список, которому не верят, хуже отсутствующего: отсутствующий заставляет проверить.

**Как получилось:** пункт закрывается не тогда, когда работа сделана, а тогда, когда кто-то вернулся его отметить. Возврата не происходит, потому что к этому моменту уже идёт следующая стадия.

**Закрывается:** пункты 120–135 сверены с тем, что произошло, и либо отмечены с указанием чем подтверждены, либо оставлены открытыми осознанно.

### Пик 258 соединений при одном пользователе не объяснён

**Риск:** 2026-08-29 панель показала пики 258 и 254 соединений в 00 и 12 часов МСК при одном известном пользователе и 6 соединениях в момент замера. За месяц этот пользователь прожёг 3.8 ГБ, что на замеры скорости похоже. Но 258 против обычных 30 — разница на порядок, и объяснения у неё нет.

Это же число — основа для «пика за неделю» и оценки мощности: если оно artefact перезапусков, а не нагрузки, то мощность считается по выдуманному пику.

**Объяснено 2026-08-29, в тот же день.** Тот же один пользователь дал 5 соединений в покое, 19 при обычной работе и 29 под нагрузкой. То есть клиент открывает соединения десятками, и 258 во время замера скорости — поведение клиента, а не след перезапусков. Мощность считается по настоящему пику.

**Что осталось:** «соединений на пользователя» на панели меняется от 5 до 29 у одного и того же человека, а мощность выражена в соединениях. Пока множитель гуляет в шесть раз, пересчёт мощности в людей — это оценка с шестикратной вилкой. Это уже отдельный пункт ниже, и он оказался серьёзнее, чем выглядел.

**Закрывается:** не требуется, пункт объяснён.

### Новую ноду невозможно проверить одним устройством

**Риск:** порядок нод для устройства считается как `uniform(deviceKey, alias) ^ (1/score)`, где `uniform` — хеш от пары «устройство + нода». Он не меняется никогда, и это сделано намеренно: план, тасующийся при каждом обновлении, переподключал бы людей без причины. Разброс достигается **между устройствами**.

При одном устройстве разброса нет вовсе. 2026-08-29 Business Owner включал и выключал VPN несколько раз и каждый раз попадал на `n-c5e6`. Вторая нода `n-481e`, собранная в тот же день, осталась с нулём сессий и доменом «не измерен» — и останется, потому что её домен некому проверить с устройства.

Следствие: **вторая нода строилась как запас, а запас, про который неизвестно, работает ли он, запасом не является.** Мы уже теряли адреса из-за блокировок; про этот неизвестно ничего.

**Обходной путь есть и не применён:** перевести работающую ноду в `draining` — она дослуживает подключённых и не выдаётся новым, так что при переподключении выбор обязан отдать вторую. Business Owner решил отложить: если новая нода заблокирована из России, на время возврата он остаётся без связи.

**Закрывается:** либо устройств становится больше одного и разброс появляется сам, либо есть способ отправить конкретное устройство на конкретную ноду для проверки, не трогая тариф и не выводя рабочую ноду из обращения.

### Сбои агента на ноде видны только в её журнале

**Риск:** агент, переставший отправлять метрики, выглядит на панели как молчащая нода, и причина требует входа на машину.
**Закрывается:** агент сообщает о своих сбоях туда же, куда отправляет метрики.

## Мощность

### Мощность считается в соединениях, а читается как в пользователях

**Риск:** `CP_NODE_CAPACITY=500` — это соединения. При одном активном пользователе живая панель показывала девять текущих соединений и пик пятьдесят пять. То есть нода держит порядка десятка человек, а не пятисот, и «занято 3%» не значит, что можно принять тысячу.
**Что сделано:** на панель добавлены соединения на пользователя, так что перевод возможен вручную.
**Закрывается:** порог считается от пользователей либо `CP_NODE_CAPACITY` задан в соединениях осознанно и это записано рядом со значением.

## Ноды

### Причина остановки nginx на n-c5e6 не установлена

**Риск:** 2026-08-30 нода оставалась доступна по management SSH и продолжала выглядеть существующей в базе, но `nginx` был `inactive`, поэтому её публичный bootstrap edge получал connection refused. Точный bootstrap URL и локальный listener теперь проверяет `Node Inspect`, а `Update Node` валидирует конфигурацию и поднимает HTTPS-сервис обратно, однако первопричина остановки из доступного безопасного журнала не восстановлена.

**Закрывается:** причина следующей остановки автоматически уходит в безопасный централизованный сигнал без client IP и request path, либо наблюдение подтверждает, что после восстановления и обновлений сервис больше не останавливается.

### Списки классификации обновляются переустановкой конфигурации ноды

**Риск:** изменение правила классификации требует прохода по всем нодам.
**Закрывается:** списки приходят с Control Plane вместе с остальной конфигурацией.

## Коммерческая Модель

### Месячная квота FREE не реализована

**Риск:** старый sizing использовал 20 GiB/мес как допущение, но server-side counter/enforcement отсутствует. Экономику нельзя защищать лимитом, которого продукт фактически не применяет.

**Закрывается:** Business Owner отдельно утверждает необходимость и значения; Core считает период, ноды отзывают credential при исчерпании, panel показывает состояние, а Android не является точкой контроля. Если квота не нужна, старые sizing-допущения окончательно удаляются.

### Production-контур платежей не оформлен

**Риск:** тестовая ЮKassa технически активирует VIP и выполняет утверждённую policy возврата, но код тестового магазина не является разрешением принимать реальные деньги. Не зафиксированы юридическое лицо и налоговый режим, договор production-магазина, обязательные данные чека/онлайн-касса, юридическая публикация политики возвратов, chargeback и бухгалтерский срок хранения payment/refund history. Цены 399 / 1 090 / 3 490 ₽ рассчитаны с инфраструктурным и комиссионным запасом, но без налогов конкретного бизнеса.

**Закрывается:** до первой реальной оплаты Business Owner утверждает юридический и фискальный контур, retention, публичный текст действующей refund policy и chargeback process; расчёт цены повторяется с фактическими налогами. После этого test shop ID и Secret Key заменяются production-значениями и выполняется production smoke payment/refund без изменения APK.

### Продление И Коммуникация Об Истечении VIP Не Спроектированы

**Риск:** платный VIP автоматически заканчивается и возвращает аккаунт в FREE, но действующий VIP пока не может купить следующий срок заранее и не получает напоминаний. Это осознанно не входит в первую платёжную стадию, однако без следующего flow пользователь узнает об окончании только по исчезновению VIP-возможностей.

**Закрывается:** отдельная стадия определяет продление, момент и каналы напоминаний, скидки/retention и поведение нескольких последовательных покупок. Подписки и автоплатежи добавляются только отдельным решением.

### Что VIP даёт, не решено

**Решено 2026-08-29 наполовину.** Business Owner назвал: у VIP нет ограничений ни на скорость, ни на устройства. Миграция `0018` это записала — `max_devices` и `max_external` у VIP теперь `null`, и `null` значит «без предела», как уже было заведено для скорости в `0017`.

**Риск (что осталось):** чем ещё VIP отличается от FREE, кроме отсутствия трёх пределов, по-прежнему не решено. Тариф, определённый через то, чего в нём нет, нечем описать покупателю.

**Закрывается:** Business Owner называет, что в VIP входит. Числа меняются одним оператором без выката.

### Внешние устройства VIP ничем не ограничены сверху

**Риск:** аккаунт VIP может выпускать `vless://` для внешних устройств без конца. Это тот же результат, что и один общий VIP-ключ, который стадия 22 запрещает, — только полученный другим путём: один купленный аккаунт раздаёт доступ дальше, и по нагрузке это видно как один очень активный пользователь.

Возражение записано в комментарии миграции `0015` и **не стало неверным — оно стало принятым**: Business Owner назвал отсутствие лимитов явно, это его решение. Пункт живёт здесь, чтобы принятое не выглядело незамеченным.

**Чего не хватает, чтобы хотя бы увидеть:** на панели нет ни числа внешних устройств на аккаунт, ни скорости их появления. Сейчас об этом узнают из счёта за трафик.

**Закрывается:** панель показывает распределение внешних устройств по аккаунтам, и становится видно, отличается ли самый активный аккаунт от остальных на порядок.

## Приёмка Платежей И Обновлений

### Чеков нет ни от ЮKassa, ни от продавца

**Риск:** магазин переведён в бой без фискализации на стороне ЮKassa — так решено осознанно, `receipt` в `Create` не нужен и не отправляется. Но чек самозанятого по НПД остаётся обязанностью продавца, а модуля, который его выбьет, ещё нет. Значит с первой настоящей продажи и до появления модуля деньги принимаются, а чек покупателю не выдаётся.

Это не техническая ошибка и не то, что чинится в коде оплаты: ЮKassa здесь ни при чём, она и не должна ничего выбивать. Записано потому, что обязанность существует с первого платежа, а не с момента, когда модуль будет готов, и разрыв между этими двумя датами — единственное, что тут можно измерить.

**Решено:** Business Owner, 2026-09-02, при переключении на боевой магазин. Отдельным заданием будет модуль к `lknpd.nalog.ru`.

**Закрывается:** чек НПД выбивается на каждый успешный платёж, и на непробитый чек есть тот же readback, что на сам платёж.

### ЮKassa не даёт test-store trigger недостаточного баланса для refund

**Риск:** воспроизводимая матрица оплаты и возврата завершена: success/failure attempt/cancel, full 399 ₽, partial 222,01 ₽, webhook repeats, exact lost-response retry, VIP/access revoke и финальные 7 дней приняты живыми readbacks. Но невозможно доказать на тестовом магазине конкретный отрицательный refund outcome `insufficient_funds`: ЮKassa документирует причину, однако не публикует способ детерминированно создать недостаточный баланс test store. Имитация такого ответа не является живой проверкой.

**Закрывается:** ЮKassa предоставляет официальный test-store trigger для refund `insufficient_funds` либо сценарий безопасно воспроизводится на production sandbox/согласованном тестовом магазине. До этого Core contract покрыт automated provider-error tests: ошибочный/незавершённый refund VIP не отключает.

### Direct APK update не пройден между двумя подписанными выпусками

**Риск:** channel-neutral latest/min policy, server-side HTTP 426, download limits, SHA-256 и PackageInstaller проверяются кодом и CI, но поведение системного installer и unknown-source permission отличается по версиям/прошивкам Android. Пока открыты общие release blockers, нельзя опубликовать даже первую официальную сборку с updater; без неё нельзя доказать переход на следующую.

**Закрывается:** после снятия release blockers публикуется первая подписанная сборка с updater, затем следующий versionCode. На реальном устройстве проходят: no update, optional/later, unavailable APK, controlled wrong hash, installer cancel/failure, required VPN block, успешная установка и восстановление VPN. Core/panel readback совпадает с каждым переключением latest/min.

## Process-Layer

### GitHub Actions принудительно запускает Node 20 actions на Node 24

**Риск:** GitHub помечает `actions/checkout@v4` и `hashicorp/setup-terraform@v3`
как использующие deprecated Node.js 20 и пока принудительно запускает их на
Node.js 24. Сейчас workflows проходят, но временная совместимость может быть
убрана платформой.

**Обнаружено:** post-merge dry-run APK-сайта `33500134659` 2026-09-01; на
результат read-only проверки предупреждение не повлияло.

**Закрывается:** зависимости обновлены до официальных версий с поддержкой
актуального GitHub Actions runtime, после чего baseline и инфраструктурные
dry-run workflows проходят без этой annotation.

### Publish-FeaturePR не создаёт первый PR и пишет literal-переменные в body

**Риск:** первая публикация новой feature branch пушит commit, но падает на обращении к `Count` у пустого результата `gh pr list`; PR приходится создавать вручную. Сгенерированный body содержит `$resolvedFeatureId` и `$branch` буквально, поэтому даже успешный путь передаёт ревьюеру неверную feature-memory.

**Обнаружено:** 2026-08-30 на первой публикации `151-bounded-user-trace`; ветка была запушена, PR №157 создан вручную с корректным body.

**Закрывается:** `Publish-FeaturePR.ps1` нормализует JSON к массиву, корректно подставляет переменные и имеет smoke-тест для пустого списка и существующего PR.

## Закрытые

Держатся здесь, а не удаляются: закрытый пункт объясняет, почему такого больше не ищут.

### Квота Kamatera на активные серверы — закрыта 2026-08-31

Provider request обработан, Add Server дважды прошёл полный production lifecycle. Старое утверждение «workflow ни разу не доходил до конца» больше не используется для оценки возможности расширения fleet.

### Тринадцать часов истории вместо недель — закрыт 2026-08-28

Казалось, что `metrics.node_samples` держит только тринадцать часов, хотя ноды работают неделями. Три чтения подряд давали «13 ч», но интервалы были слишком малы, чтобы округление до целого что-то показало.

Через час размах стал «14 ч». История растёт нормально; тринадцать часов были просто её возрастом. Ничего не чистит таблицу.

### Нет PostgreSQL backup и проверенного restore — закрыт 2026-09-01

Был главным незакрытым риском: аккаунты, планы, entitlement, платежи, fleet и история панели лежали на одном диске с Core, и копии не было.

Закрыт не тем, что копии появились, а тем, что **восстановление выполнено в бой и подтверждено панелью**. Ежедневная выгрузка и выгрузка перед выкатом Core, шифрование `age` на три ключа, из которых конвейер не держит ни одного пользовательского, репетиция во временную базу, восстановление в бой за одобрением и кнопка отката.

Проверка на живой базе нашла четыре дефекта, каждый из которых иначе всплыл бы в день настоящей аварии: право владения после `--no-owner`, юнит в systemd backoff, шаг, ждущий пароль без TTY, и проверка здоровья, ждущая не тот код. Все четыре — в самом инструменте восстановления, ни один не был виден до выката.

### Переписанная проверка здоровья не отвечала живому сервису — закрыт 2026-09-02

Проверка `/healthz` была переписана дважды подряд — тело вместо кода, затем одна копия вместо трёх — и стояла в трёх workflow, ни разу не выполнившись после правок.

Закрыт первым же выкатом Core, шагом `Prove it answers`: `The service answers: {"status":"ok"}`. Отдельного прогона не потребовалось, потребовалось посмотреть на этот шаг, когда он случился.

### Двенадцать удалённых серверов считались как «заменить» — закрыт 2026-09-02

Вердикт «требует замены» считался только по состоянию, без жизненного цикла. Снятая нода сохраняет то состояние, в котором её видели последним, больше её никто не измеряет, и «неисправна дольше часа» оставалось истиной навсегда.

Закрыт правкой вердикта, отдельным столбцом «уже нет» и инвариантом «снятое — не работа» по всем сочетаниям жизненного цикла и состояния. Проверено на панели после выката: у серверов «заменить 0, удалить 0, уже нет 12», при этом домен, требующий замены по делу, помеченным остался.

Стоит запомнить не саму правку, а то, почему её не поймал тест: случай `removed` в таблице был один, и с состоянием `Unknown`, при котором ошибочная ветка недостижима. Проверка существовала, выглядела покрывающей и не могла упасть.
