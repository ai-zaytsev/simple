# DigitalOcean

Провайдер расходуемого флота и объектного хранилища. Решение — `ADR-005`, топология и бюджет — [../architecture/mvp-topology.md](../architecture/mvp-topology.md).

Проект: `simple-vpn-prod`. API: `https://api.digitalocean.com/v2`, авторизация `Authorization: Bearer`.

## Секреты

| Имя | Что это |
| --- | --- |
| `DIGITALOCEAN_TOKEN` | API-токен, намеренно сужен |
| `SPACES_ACCESS_KEY_ID` | Ключ доступа к Spaces |
| `SPACES_SECRET_ACCESS_KEY` | Секрет доступа к Spaces |

## Фактическая Область Токена

Установлено запуском `Infra Inventory` 2026-08-22.

| Endpoint | Ответ | Вывод |
| --- | --- | --- |
| `GET /v2/account` | 403 | Права нет, и это правильно: эндпоинт отдаёт email и данные владельца |
| `GET /v2/projects` | 200 | Проект `simple-vpn-prod` виден |
| `GET /v2/droplets` | 200 | Дроплетов сейчас 0 |
| `GET /v2/account/keys` | 200 | SSH-ключи читаются |
| `GET /v2/tags` | 200 | |
| `GET /v2/regions` | 200 | |
| `GET /v2/sizes` | 200 | |
| `POST /v2/tags` и `DELETE /v2/tags/{name}` | 201, 204 | Право записи есть |

Токена достаточно для provisioning. Дополнительных прав запрашивать не требуется.

## Правило Проверки Доступа

**Работоспособность токена проверяется теми эндпоинтами, которые реально используются, и никогда — эндпоинтом идентичности или метаданных.**

Это правило появилось после двух одинаковых ошибок:

- Cloudflare: проверка через `GET /user/tokens/verify` дала `401` на полностью рабочем токене, потому что он не имел права читать метаданные самого себя
- DigitalOcean: проверка через `GET /v2/account` дала `403` на полностью рабочем токене, потому что он не имел права читать данные аккаунта

В обоих случаях правильно суженный токен выглядел сломанным. Проверка, наказывающая за корректное ограничение прав, хуже отсутствия проверки: она провоцирует расширить права, чтобы «починить» диагностику.

Право записи проверяется созданием и немедленным удалением тега. Теги бесплатны и не несут ресурсов — это единственный способ узнать про запись, не создав платный объект.

## Spaces

| Параметр | Значение |
| --- | --- |
| Bucket | `simple-vpn-infra-backups` |
| Регион | `ams3` |
| Endpoint | `https://ams3.digitaloceanspaces.com` |
| Стоимость | $5/мес, включает 250 GB и 1 TB исходящего трафика |

Дополнительные bucket не создаются. Оба назначения живут в одном:

| Префикс | Назначение |
| --- | --- |
| `terraform/` | Remote state Terraform |
| `postgres/` | Бэкапы PostgreSQL |
| `healthcheck/` | Временные объекты проверки, удаляются сразу |

### Шифрование

DigitalOcean шифрует данные Spaces на диске алгоритмом AES-256 по умолчанию и не предоставляет настройку шифрования на уровне bucket. Поэтому `get-bucket-encryption` отвечает ошибкой, и это не признак проблемы. Требование о шифровании выполняется платформой.

Дополнительно: бэкапы PostgreSQL шифруются на стороне Core до загрузки. Шифрование провайдером защищает от изъятия диска у провайдера, но не от доступа к самому bucket по ключам.

### Versioning И Retention

| Настройка | Значение | Зачем |
| --- | --- | --- |
| Versioning | Enabled | Повреждённый state Terraform восстанавливается только из предыдущей версии |
| `terraform/`, старые версии | 90 дней | Хватает откатить неудачный apply, мало чтобы копить |
| `postgres/`, текущие объекты | 30 дней | Минимально достаточно для единственных невосстановимых данных |
| `postgres/`, старые версии | 7 дней | Версии нужны против затирания, а не как история |
| Незавершённые multipart | 7 дней | Обрывы загрузок тарифицируются и не видны в листинге |

Настраивается workflow `Spaces Configure`. Он же выполняет round-trip: запись, чтение обратно, удаление. Хранилище, в которое ни разу не писали, целью бэкапа не является.

### Почему Bucket Настраивается Не Через Terraform

В этом bucket лежит state самого Terraform. Управлять им из Terraform — цикл начальной загрузки. Хранилище состояния настраивается вне инструмента, чьё состояние оно хранит.

## Границы Бюджета

Расходы DigitalOcean, включая Spaces, не превышают $45/мес. Плановая конфигурация — $31/мес, см. [../architecture/mvp-topology.md](../architecture/mvp-topology.md).

- autoscaling и любое автоматическое увеличение платных ресурсов не настраиваются
- добавление дроплета сверх плановых — решение Business Owner
- при приближении расхода трафика к 3000 GiB срабатывает алерт, а не расширение
